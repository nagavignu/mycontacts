const contacts = (state, {type, payload}) => {
    switch (type) {
        case "contacts":
            return state;
            break;
    
        default:
            return state;
            break;
    }
}

export default contacts;